import 'package:flutter/material.dart';

class SMBFCard extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(
          title: const Text('SMBF Card'),
        ),
        body: Container(child: Text('SMBFCard')));
  }
}